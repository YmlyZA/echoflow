{
  "name": "@echoflow/backend",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx --conditions=echoflow-source src/dev.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/ws": "^8.18.1",
    "tsx": "^4.22.3",
    "typescript": "^5.8.0",
    "vite": "^7.3.3",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@echoflow/protocol": "workspace:^",
    "@fastify/websocket": "^11.2.0",
    "fastify": "^5.8.5",
    "ws": "^8.21.0"
  }
}
